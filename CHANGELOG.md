# 更新日志

## v2.4.0 (2026-09-24)

- 新增「账号」：顶栏「账号」入口，登录后可查看头像、昵称、等级称号、经验进度、收藏数（已用 / 上限）与 J 币
- 新增「收藏夹」：账号页可折叠预览收藏，点「查看更多」进入完整收藏页，支持按收藏夹筛选与翻页；
  本子详情页新增「收藏 / 取消收藏」，收藏时选择要加入的收藏夹
- 新增「每日签到」：账号页一键签到，弹窗展示签到状态、当月签到天数、连续签到天数与当天奖励
- 账号密码改为本机加密保存（AES-256-GCM，与本机标识绑定），配置文件与磁盘上都不再出现明文；
  账号页「退出登录」可清除本机保存的账号信息
- 默认外观改为深色模式（仍可在设置页切换 浅色 / 深色 / 跟随系统）
- 新增安全说明文档 `SECURITY.md`，说明凭据处理方式与安全边界

## v2.3.1 (2026-09-23)

- 新增「浏览」：在资源管理器里选中已下载的 PDF 或图片文件夹，即可在软件内直接翻阅，
  不必再开外部程序 —— 支持横式 / 竖式切换、左右翻页与滚轮滚动，
  右下角显示页码（PDF）或图片名称（图片文件夹），右上角关闭
- 新增「在线浏览」：本子详情页点「浏览」可直接在线看完整本子（同一套阅读界面），不必先下载；
  取图按需进行并预取后续页面，页数多时按段加载
- 本子详情页进入即**自动**展示前 5 页预览（原来需要点按钮），点击可放大查看，
  放大后可用两侧按钮在这几张之间逐张翻阅
- 首页新增欢迎语「今天想看点什么？」
- 窗口标题精简为「jm2pdf v2.3.1 - by WisadelZ」（去掉中文说明，保留作者署名）

## v2.3.0 (2026-09-23)

- 首页改为「探索」：打开软件就能直接搜本子；下载移到顶栏「下载」按钮进入（左上角可返回首页）
- 搜索更好用：新增「全部」范围（作品 / 作者 / 标签 / 角色一起搜）并作为默认；
  搜索框右侧新增清除按钮；结果页左上角的返回按钮可一键清空关键词与结果、回到开屏界面
- 结果可排序与筛选：最新 / 观看数 / 图片数 / 点赞数，或按日期筛选（今天 / 本周 / 本月 / 全部）
- 本子详情页新增「点赞数」「观看数」展示


## v2.2.0 (2026-09-22)

- 新增「探索」页（顶栏按钮，与资源管理器 / 帮助 / 设置并列）：
  - 顶部居中搜索框，左侧可切换搜索方式：作品 / 作者 / 标签 / 角色
  - 关键词支持站内语法：`+词` 必须包含、`-词` 必须排除
  - 每页 20 个结果，格子尺寸固定（封面 3:4 + 两行名称），按窗口宽度自动换行
  - 点击封面放大查看：背景虚化变暗，右上方关闭按钮
  - 点击名称进入本子详情页；结果上下各有一套翻页工具，浏览方向不受限
  - 从详情页返回时保留上一次搜索结果
- 新增「本子详情」页：左上方大封面，右上方本子信息（标题 / 本子 ID / 作者 / 标签 / 页数 / 章节数），
  下方提供「跳转官网」「直接下载」「加入下载列表」三个操作
- 封面图只在内存中展示、不落盘；取图失败时显示占位图标
- 探索页封面按本子 ID 并发获取（8 线程），避免逐张等待拖慢结果展示
- 三语文本表补齐探索页与详情页全部文案

## v2.1.2 (2026-09-22)

- 搜索结果显示封面：搜索结果框右侧新增封面缩略图（3:4，取自网站 `_3x4` 缩略图），
  图片只在内存中展示，不写入磁盘；取图失败时显示占位图标，不影响搜索本身
- 文本表新增「封面加载失败」占位提示（三语）

## v2.1.1 (2026-09-22)

- 新增「资源管理器」页（顶栏按钮，与「设置」并列）：
  - 按漫画归组管理下载目录，同一本漫画的 PDF 与图片文件夹用折叠菜单归在一起
  - 勾选后可批量「打开」或「删除」；删除为移入回收站，可从回收站恢复
  - 勾选 PDF 时右侧边栏展示其元数据：标题 / 本子 ID / 作者 / 标签 / 页数 / 章节序号
- 新增「帮助」页（顶栏按钮）：展示版本号、作者署名、项目地址、分发协议与免责声明，
  并引导前往 GitHub 项目主页提交 Issue
- 生成 PDF 时写入文件元数据（DocInfo）：Title 为漫画名称、Author 为作者、
  Subject 为标签、Keywords 为 `id/pages/chapter`、Creator 为程序版本；
  图片文件不做任何处理（不写 EXIF、不重新编码）
- `conf.yml` 中 `after_photo` 的插件由 `img2pdf` 改为 `jm2pdf_meta_pdf`
  （旧配置里的 `img2pdf` 会被自动识别并改写，无需手动修改）
- 新增 `core/library.py`（下载目录扫描 / 打开 / 移入回收站）、`core/pdf_metadata.py`
  （元数据映射、写入与读取）；三语文本表补齐新页面全部文案

## v2.1.0 (2026-09-22)

- 架构重构：单文件 app.py 拆分为 `core` / `ui` / `utils` 三层，app.py 仅保留入口，
  各层职责单一，新增功能时只需在对应层增加模块，便于维护
- 新增设置页（主界面左上角「设置」按钮进入），包含三个分区：
  - 配置管理：导出当前配置到文件备份、从文件导入配置（导入后界面立即刷新）
  - 外观设置：浅色 / 深色 / 跟随系统，切换后立即生效
  - 语言设置：简体中文 / 繁体中文 / English，切换后立即生效
- 新增多语言支持，界面全部文本（含下载日志与状态提示）均纳入三语文本表
- 新增主题适配，界面配色改用 Material 3 语义色，深浅色下自动适配
- 界面字体统一为「微软雅黑 UI」，修正中英文混排时字重忽粗忽细的问题
- 修正主页首个输入框浮动标签被滚动容器裁剪的问题
- 生成 PDF 的文件名改为与图片文件夹同名（取 `dir_rule` 的最后一段，默认 `Pname`），
  按名称排序时 PDF 与其图片文件夹紧挨在一起，便于管理
- 搜索结果末尾附加本子网页链接，点击即可用浏览器打开（固定使用 18comic.vip 域名）
- 配置文件新增 `app.theme_mode`、`app.language` 两项
- 打包脚本改为从 `core/constants.py` 读取版本号

## v2.0.0 (2026-09-21)

- UI 重构：由 tkinter 迁移至 Flet 框架，界面更现代、紧凑（默认窗口 660x700）
- 新增：主界面「生成 PDF」开关，可自由选择是否将章节合并为 PDF
- 下载选项与邮件推送改为折叠面板，默认收起以减小页面面积
- 搜索结果框改为可滚动文本区，长内容自动换行无需拖动
- 打包方式改为 flet pack（单文件 exe），并写入产品版本与版权信息
- 配置同步、搜索、批量下载、邮件推送等核心功能保持不变

## v1.2.0 (2026-09-21)

- 下载选项 / 邮件推送改为折叠式面板，未勾选时固定折叠
- 窗口宽度缩小，输入框随窗口自适应
- 搜索结果顺序调整为 ID / 页数·章节 / 名称 / 标签，自动换行
- 所有控件响应窗口缩放

## v1.1.0 (2026-09-21)

- 全部下载与邮件选项纳入 conf.yml，界面修改自动同步写回
- 新增本子搜索（ID → 名称 / 页数 / 章节 / 标签）与一键添加到下载列表
- exe 按版本号命名

## v1.0.0 (2026-09-21)

- 首个版本：ID 下载、img2pdf 合并、邮件推送、tkinter 图形界面
